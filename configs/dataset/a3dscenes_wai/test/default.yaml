dataset_str:
  "A3DScenesWAI(
    split='${dataset.a3dscenes_wai.test.split}',
    resolution=${dataset.a3dscenes_wai.test.dataset_resolution},
    principal_point_centered=${dataset.a3dscenes_wai.test.principal_point_centered},
    seed=${dataset.a3dscenes_wai.test.seed},
    transform='${dataset.a3dscenes_wai.test.transform}',
    data_norm_type='${dataset.a3dscenes_wai.test.data_norm_type}',
    ROOT='${dataset.a3dscenes_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.a3dscenes_wai.test.dataset_metadata_dir}',
    overfit_num_sets=${dataset.a3dscenes_wai.test.overfit_num_sets},
    variable_num_views=${dataset.a3dscenes_wai.test.variable_num_views},
    num_views=${dataset.a3dscenes_wai.test.num_views},
    covisibility_thres=${dataset.a3dscenes_wai.test.covisibility_thres},
    covisibility_thres_max=${dataset.a3dscenes_wai.test.covisibility_thres_max},
    sampling_mode='${dataset.a3dscenes_wai.test.sampling_mode}',
    load_modalities=${dataset.a3dscenes_wai.test.load_modalities},)"
split: 'test'
dataset_resolution: ${dataset.resolution_test_a3dscenes}
principal_point_centered: ${dataset.principal_point_centered}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/a3dscenes
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
overfit_num_sets: null
variable_num_views: ${dataset.test.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: ${dataset.covisibility_thres_min}
covisibility_thres_max: ${dataset.covisibility_thres_max}
sampling_mode: ${dataset.sampling_mode}
load_modalities: ["image", "depth", "mask"]
